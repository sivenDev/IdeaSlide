{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allows the main IdeaNote window to check, download, and install signed updates.",
  "windows": ["main"],
  "permissions": ["updater:default"]
}
